specVersion: "v1"
serviceId: "audio"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "audio-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
